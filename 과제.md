본 과제는 학습 플랫폼의 핵심 기능인 '단원별 문제 풀이' 및 '풀이 이력 조회' API를 구축하는 것입니다. 단순 기능을 넘어 객체지향적 설계와 데이터 처리 성능을 고려한 구현 능력을 평가합니다.

기술 스택 : java 21, spring, jpa, mysql 를 사용

- 이때 명시되지 않은 버전은 편하신 버전을 사용하시면 됩니다.

## 기능 요구사항

## 1. 단원별 문제 풀이

### 1.1 랜덤 문제 조회

- 사용자가 단원을 선택하면 해당 단원의 문제를 **하나씩** 제공한다.
- 현재 객관식 문제 선택지는 총 5개 주어진다. (현재는 5지선다)
- 문제는 복수 정답/단일 정답 문제가 따로 존재한다.
- 사용자가 풀지 않은 문제에서 문제를 1개 랜덤 조회한다.
- '문제 넘기기' 버튼을 누르면 현재 문제를 풀지 않고 같은 챕터 내의 새로운 문제를 랜덤으로 제공받는다. 단, 직전에 건너뛴 문제는 다음 문제 랜덤 추출 대상에서 제외된다.
    - 특정 사용자의 특정 chapter에 대해 바로 직전에(1회 전) 건너뛴 문제 제외
- 정답률 계산
    - 동일 문제를 30명 이상 사용자가 해당 문제를 푼 경우에만 해당 문제에 대한 평균 정답률 데이터를 제공한다.
    - 부분 정답의 경우 정답률 계산에서는 틀린 것으로 간주한다.
    - 30명 이상 푼 문제: 소수점 첫째 자리에서 반올림 (예: 66.7% → 67%)
    - 30명 미만: `null` 반환
- 해당 단원의 모든 문제를 다 풀었거나 더 이상 제공할 문제가 없는 경우의 예외 처리(Exception Handling) 또는 적절한 응답 코드를 고민하여 구현해 주세요
- request 예시

```json
{ 
	"chapterId": 1,
	"userId": 1
}
```

- response 에 꼭 필요한 값 - 아래 외의 값은 자유롭게 설정하면 됩니다.

```json
{ 
	"problemId": 1, 
	"content": "문제 설명", 
	"choices": ["지문1", "지문2", ...], 
	"answerCorrectRate": 23
}
```

### 1.2 문제 제출

- 객관식: 선택한 선택지 번호들 제출 (복수 정답 가능)
- 주관식: 텍스트로 답안 제출
- 사용자가 제출한 답에 따라 정답/오답/복수 정답이 가능하다.
    - 부분 정답이 가능하다
        - 오답 포함 여부와 상관없이 정답을 1개라도 포함했다면 부분 정답입니다
            - [1,2] 가 정답일 때 [1,3]을 제출해도 부분 정답 처리 된다.
- 제출 즉시 정답 여부와 해설이 반환 된다.
- request 예시

```json
{ 
	"problemId": 1, 
	"userId": 1,
	"answerType": "주관식", 
	"userAnswer": ... <- 타입 기호에 맞게 설정
}
```

- response 예시 - 아래 외의 값은 자유롭게 설정하면 됩니다.

```json
{ 
	"problemId": 1, 
	"answerStatus": "correct", // 정답, 부분 정답, 오답 
	"explaination": "문제 해설...", 
	"problemAnswers": ... <- 타입 기호에 맞게 설정
}
```

## 2. 풀었던 문제 상세

- 사용자는 자신이 풀었던 단원별 문제 풀이를 상세 조회할 수 있다.
- request 예시

```json
{ 
	"userId": 1, 
	"problemId": 3
}
```

- response 예시 - 아래 외의 값은 자유롭게 설정하면 됩니다.

```json
{ 
	"problemId": 1, 
	"answerStatus": "correct", // 정답, 부분 정답, 오답 
	"explaination": "문제 해설...", 
	"problemAnswers": ... <- 타입 기호에 맞게 설정, 
	"userAnswers": ... <- 타입 기호에 맞게 설정, 
	"answerCorrectRate" : 23
}
```

### 추가 설명

- ai를 사용해서 개발해도 상관 없습니다.
- request/response 형식을 자유롭게 변경해도 좋습니다.
- 적절한 input/request validation이 필요합니다.

## 우대사항

- 아키텍처 및 설계
    - 멀티 모듈 아키텍처를 활용한 설계
    - DDD(Domain Driven Design) 원칙을 준수
    - 객체 지향적인 / 테스트 하기 쉬운 코드 작성
- 테스트 및 문서화
    - 적절한 단위 테스트 및 통합 테스트 작성
    - swagger 문서화 및 README.md 작성을 통한 프로젝트 설명
- 성능
    - 정답률 계산 로직의 효율성
    - 쿼리 성능
- 인프라 및 환경
    - Docker Compose를 활용해 애플리케이션과 DB 환경을 한 번에 구동 가능하도록 구성

이 우대사항 외에 추가로 개발하거나 고려한 것이 있다면 README.md 에 작성해주셔도 좋습니다.

### 제출 안내

- **기한:** 시작일로부터 48시간 이내
- **제출물:** GitHub Repository URL (Public) 및 설계 의도가 담긴 README.md